import Link from "next/link";
const content: Record<string, [string, string]> = { features: ["Features", "Local connection profiles, extraction workflows, validation, and Excel exports."], "how-it-works": ["How it works", "The Windows app connects locally to TallyPrime using configured supported interfaces."], download: ["Download", "Tally Prime Connector is for Windows. Downloads will be published here."], documentation: ["Documentation", "Configuration and integration documentation will be published here."], support: ["Support", "Get help with installation, connection diagnostics, and exports."], privacy: ["Privacy", "Your accounting data remains on your computer unless you choose to export it."], terms: ["Terms", "Terms of use will be published here."] };
export function generateStaticParams() { return Object.keys(content).map(page => ({ page: [page] })); }
export default async function Page({ params }: { params: Promise<{ page: string[] }> }) { const { page } = await params; const [title, text] = content[page.join("/")] ?? ["Not found", "This page does not exist."]; return <section className="py-20"><h1 className="text-4xl font-bold">{title}</h1><p className="mt-5 text-slate-300">{text}</p><Link className="mt-8 inline-block text-cyan-300" href="/">Back home</Link></section>; }
